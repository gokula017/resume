<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Professional Resume</title>
    <link href="/css/style.css" rel="stylesheet" />
</head>

<body>
    <%- include('partials/header.ejs'); %>
        <div class="container view-resume-page">
            <div class="basic-info">
                <h1>
                    <%= result.name %>
                </h1>
                <p>Email: <%= result.email %> | Phone: <%= result.mobile %> | Location: City, Country</p>
                <p>LinkedIn: <a href="<%= result.linkedin %>">
                        <%= result.linkedin %>
                    </a> | GitHub: <a href="<%= result.github %>">
                        <%= result.github %>
                    </a></p>
            </div>

            <section>
                <h2>Profile Summary</h2>
                <p>
                    <%= result.profileSummary %>
                </p>
            </section>

            <section>
                <h2>Work Experience</h2>
                <div class="job">
                    <h3>Senior Frontend Developer – ABC Tech Pvt Ltd</h3>
                    <span>Jan 2020 – Present | Bengaluru, India</span>
                    <ul>
                        <li>Led the development of responsive React applications for clients in e-commerce and finance.
                        </li>
                        <li>Collaborated with backend teams to integrate REST APIs and enhance performance.</li>
                        <li>Mentored junior developers and conducted code reviews for quality assurance.</li>
                    </ul>
                </div>
                <div class="job">
                    <h3>UI Developer – XYZ Solutions</h3>
                    <span>Jun 2016 – Dec 2019 | Mumbai, India</span>
                    <ul>
                        <li>Developed pixel-perfect UIs using HTML, CSS, and JavaScript frameworks.</li>
                        <li>Optimized website performance, reducing load time by 30%.</li>
                        <li>Worked closely with designers to create immersive and accessible user experiences.</li>
                    </ul>
                </div>
            </section>

            <section>
                <h2>Education</h2>
                <div class="edu">
                    <h3>Bachelor of Technology in Computer Science</h3>
                    <span>XYZ University | 2012 – 2016</span>
                </div>
            </section>

            <section>
                <h2>Skills</h2>
                <div class="skills-list">
                    <% if(result.skills){ result.skills?.split(',').forEach(skill=> { %>
                        <span>
                            <%= skill.trim() %>
                        </span>
                        <% }) }%>
                </div>
            </section>

            <section>
                <h2>Projects</h2>

                <% if(result.projects){result.projects.forEach(project=>{ %>

                    <div class="project">
                        <h3>
                            <%= project.name %>
                        </h3>
                        <p>
                            <%= project.description %>
                        </p>
                    </div>

                    <% }) } %>
            </section>


            <section>
                <h2>Certifications</h2>
                <ul>
                    <% if(result.certifications){ result.certifications.split(',').forEach(cert=> { %>
                        <li>
                            <%= cert.trim() %>
                        </li>
                        <% })} %>
                </ul>
            </section>

            <section>
                <h2>Languages</h2>
                <ul>
                    <% if(result.languages){result.languages.split(',').forEach(lang=> { %>
                        <li>
                            <%= lang.trim() %>
                        </li>
                        <% }) } %>
                </ul>
            </section>

            <section>
                <h2>Hobbies</h2>
                <ul>
                    <% if(result.hobbies){result.hobbies.split(',').forEach(hobby=> { %>
                        <li>
                            <%= hobby.trim() %>
                        </li>
                        <% }) }%>
                </ul>
            </section>
        </div>
</body>

</html>